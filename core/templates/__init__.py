"""Package marker so importlib.resources can locate bundled assets.

Files under this package (notably ``graph.html``) ship as setuptools
package-data per pyproject.toml ``[tool.setuptools.package-data]``.
"""
